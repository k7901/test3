

<div class= "container">
  <div class= "row">

    <div class="col-md-3 ">
        <%= render 'lists', book: Book.new, user: @user  %>
    </div>
    <div class="col-md-8 offset-md-1">
      <h2>Books Detail</h2>
        <table class="table table-hover table-inverse">
          <thead>
           
          </thead>
          <tbody>
           
           <th><%= image_tag @user.get_profile_image %><br> <%= @user.name %> </th>　 
          
              <th><%= @book.title %></th>
              <th><%= @book.body %></th>
                
              
              <th><%= link_to 'Edit', edit_book_path, class: "btn btn-success btn-sm" %>
              </th>
              <th>
                <%= link_to 'Destroy', book_path, class: "btn btn-danger btn-sm" %>
              
              </th>
                
             
          
          </tbody>
        </table>
    </div>
  </div>
</div>