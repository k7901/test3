


<div class="container">
  <div class="row">
    <div class="col-m-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
  		<h1>Editing Book</h1>
    	  

    	  <% if @book.errors.any? %>
        <%= @book.errors.count %>errors prohibited this book from being saved:
        <ul>
         <% @book.errors.full_messages.each do |message| %>
           <li><%= message %></li>
         <% end %>
        </ul>
        <% end %>

         <%= form_with model: @book do |f| %>

           <h4>Title</h4>
           <%= f.text_field :title , size: "50x40" %>

           <h4>Opinion</h4>
           <%= f.text_area :body , size: "50x3" %>



          <%= f.submit "Update Book", class: "btn btn-success btn-sm" %>


         <% end %>

         <br>
         <%= link_to 'Show', book_path, class: "edit_link btn-sm" %>
         |
          <%= link_to 'Back', books_path, class: " edit_link btn-sm" %>
       
    </div>
  </div>
</div>