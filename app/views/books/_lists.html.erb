

<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-12">
      <h2>User info</h2>
      <div> <%= image_tag @user.get_profile_image %></div>
        <table class="table">
          <tbody>

            <tr>
	            <th>name</th>
	            <th><%= @user.name %></th>
	          </tr>


	          <tr>
	            <th>introduction</th>
	            <th><%= @user.introduction %></th>
	          </tr>

          </tbody>
        </table>
   
          <div class="row">
           
              <%= link_to  edit_user_path(@user.id),class:"btn btn-outline-secondary btn-block" do %>
                <i class="fa-solid fa-user-gear"></i>
              <% end %>
            
          </div>

      <h2>New Book</h2>

        <% if book.errors.any? %>
        <%= book.errors.count %>errors prohibited this book from being saved:
        <ul>
         <% book.errors.full_messages.each do |message| %>
           <li><%= message %></li>
         <% end %>
        </ul>
        <% end %>

        <%= form_with model: book do |f| %>

        <h4>Title</h4>
        <%= f.text_field :title %>

        <h4>Opinion</h4>
        <%= f.text_area :body %>

       
       <%= link_to 'Create Book', books_path, class: "btn btn-success btn-sm" %>
        <% end %>

　　 </div>
  </div>
</div>